import React from 'react';
import logo from '../../';

const Header = () => {
    return (
        <div>
            <h3>I am Header.</h3>
            <img src={logo} alt="" />
        </div>
    );
};

export default Header;